---
import { Content as WelcomeContent } from '../content/docs/index.mdx';
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import Search from "astro-pagefind/components/Search";
---

<StarlightPage 
  frontmatter={{
    title: "Helper.DB - Documentação Completa",
    description: "Biblioteca JavaScript poderosa para gerenciamento de dados com suporte a múltiplos drivers de banco de dados.",
    template: "splash"
  }}
>
  <div class="hero-section">
    <div class="hero-content">
      <h1 class="hero-title">
        <span class="text-gradient">Helper.DB</span>
      </h1>
      <p class="hero-subtitle">
        Biblioteca JavaScript poderosa e intuitiva para gerenciamento de dados
      </p>
      <p class="hero-description">
        Simplifique suas operações de banco de dados com uma API unificada que suporta 
        JSON, SQLite, MySQL, MariaDB, MongoDB e muito mais.
      </p>

      <!-- Componente de busca integrado -->
      <div class="search-container">
        <h3>🔍 Buscar na Documentação</h3>
        <Search 
          id="search" 
          className="pagefind-ui" 
          uiOptions={{
            showImages: false,
            placeholder: "Busque por métodos, exemplos...",
            translations: {
              placeholder: "Buscar documentação...",
              clear_search: "Limpar busca",
              load_more: "Carregar mais resultados",
              search_label: "Buscar neste site",
              filters_label: "Filtros",
              zero_results: "Nenhum resultado para [SEARCH_TERM]",
              many_results: "[COUNT] resultados para [SEARCH_TERM]",
              one_result: "[COUNT] resultado para [SEARCH_TERM]",
              alt_search: "Nenhum resultado para [SEARCH_TERM]. Mostrando resultados para [DIFFERENT_TERM]",
              search_suggestion: "Para buscar por [SEARCH_TERM], tente:",
              searching: "Buscando por [SEARCH_TERM]..."
            }
          }}
        />
      </div>

      <div class="features-grid">
        <div class="feature-card">
          <h3>🚀 Múltiplos Drivers</h3>
          <p>Suporte para JSON, SQLite, MySQL, MariaDB, MongoDB e Memory</p>
        </div>

        <div class="feature-card">
          <h3>⚡ Alta Performance</h3>
          <p>Operações otimizadas com cache e transações</p>
        </div>

        <div class="feature-card">
          <h3>🔧 API Simples</h3>
          <p>Interface intuitiva e consistente para todas as operações</p>
        </div>

        <div class="feature-card">
          <h3>🛡️ Backup Automático</h3>
          <p>Sistema de backup e restore integrado</p>
        </div>

        <div class="feature-card">
          <h3>📊 Estatísticas</h3>
          <p>Funções avançadas de agregação e análise</p>
        </div>

        <div class="feature-card">
          <h3>🔍 Busca Avançada</h3>
          <p>Sistema de busca poderoso com regex e filtros</p>
        </div>
      </div>

      <div class="quick-start">
        <h3>Início Rápido</h3>
        <pre><code>npm install helper.db

// Exemplo básico
const db = require('helper.db');

// Salvar dados
db.set('usuario', { nome: 'João', idade: 25 });

// Recuperar dados
const usuario = db.get('usuario');
console.log(usuario); // { nome: 'João', idade: 25 }</code></pre>
      </div>
    </div>
  </div>
</StarlightPage>

<style>
  .hero-section {
    text-align: center;
    padding: 2rem 1rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .text-gradient {
    background: linear-gradient(135deg, var(--sl-color-accent), var(--sl-color-accent-high));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.5rem;
    color: var(--sl-color-gray-6);
    margin-bottom: 1rem;
  }

  .hero-description {
    font-size: 1.1rem;
    color: var(--sl-color-gray-5);
    margin-bottom: 3rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .search-container {
    background: var(--sl-color-gray-1);
    border: 1px solid var(--sl-color-gray-3);
    border-radius: 1rem;
    padding: 2rem;
    margin: 2rem 0 3rem 0;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .search-container h3 {
    margin-top: 0;
    color: var(--sl-color-accent);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
    margin: 3rem 0;
  }

  .feature-card {
    background: var(--sl-color-white);
    border: 1px solid var(--sl-color-gray-3);
    border-radius: 0.75rem;
    padding: 1.5rem;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
  }

  .feature-card h3 {
    color: var(--sl-color-accent);
    margin-top: 0;
    margin-bottom: 0.5rem;
  }

  .feature-card p {
    color: var(--sl-color-gray-6);
    margin: 0;
  }

  .quick-start {
    background: var(--sl-color-gray-1);
    border-radius: 1rem;
    padding: 2rem;
    margin-top: 3rem;
    text-align: left;
  }

  .quick-start h3 {
    text-align: center;
    color: var(--sl-color-accent);
    margin-top: 0;
  }

  .quick-start pre {
    background: var(--sl-color-gray-2);
    border: 1px solid var(--sl-color-gray-3);
    border-radius: 0.5rem;
    padding: 1rem;
    overflow-x: auto;
  }

  .quick-start code {
    font-family: 'Fira Code', 'Consolas', monospace;
    font-size: 0.875rem;
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }

    .features-grid {
      grid-template-columns: 1fr;
    }

    .search-container,
    .quick-start {
      margin-left: 1rem;
      margin-right: 1rem;
    }
  }
</style>