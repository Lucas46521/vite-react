
---
import Layout from '../../layouts/Layout.astro';
import { docsStructure } from '../../layouts/lib/docsStructure.ts';
---

<Layout title="Documentação - Helper.DB">
  <div class="container mx-auto px-4 py-8">
    <header class="text-center mb-12">
      <h1 class="text-4xl font-bold text-gray-900 mb-4">Documentação Helper.DB</h1>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        Explore todas as funcionalidades da Helper.DB - uma biblioteca completa para gerenciamento 
        de dados com suporte a múltiplos drivers e operações avançadas.
      </p>
    </header>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
      {docsStructure.map((category) => (
        <div class="bg-white rounded-lg shadow-md border">
          <div class="p-6 border-b">
            <div class="flex items-center mb-2">
              <span class="text-3xl mr-4">{category.icon}</span>
              <h2 class="text-2xl font-bold text-gray-800">{category.title}</h2>
            </div>
            <p class="text-gray-600">{category.description}</p>
          </div>
          <div class="p-6">
            <div class="grid grid-cols-1 gap-2">
              {category.items.map((item) => (
                <a 
                  href={`/docs/${category.slug}/${item.slug}`}
                  class="flex items-center p-3 rounded-lg hover:bg-gray-50 transition-colors"
                >
                  <span class="text-blue-600 hover:text-blue-800 font-medium">
                    {item.title}
                  </span>
                </a>
              ))}
            </div>
          </div>
        </div>
      ))}
    </div>

    <div class="mt-16 bg-gradient-to-r from-blue-50 to-indigo-50 rounded-lg p-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-4">Começando Rapidamente</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="text-center">
          <div class="text-4xl mb-3">📦</div>
          <h3 class="font-semibold mb-2">Instalação</h3>
          <p class="text-gray-600 text-sm">Instale via npm ou yarn</p>
        </div>
        <div class="text-center">
          <div class="text-4xl mb-3">⚡</div>
          <h3 class="font-semibold mb-2">Configuração</h3>
          <p class="text-gray-600 text-sm">Configure seu driver preferido</p>
        </div>
        <div class="text-center">
          <div class="text-4xl mb-3">🚀</div>
          <h3 class="font-semibold mb-2">Uso</h3>
          <p class="text-gray-600 text-sm">Comece a usar os métodos</p>
        </div>
      </div>
    </div>
  </div>
</Layout>
</styled>
---
import Layout from '../../layouts/Layout.astro';
import { docsStructure } from '../../layouts/lib/docsStructure';
---

<Layout title="Documentação - Helper.DB">
  <main class="container mx-auto px-4 py-8">
    <div class="max-w-6xl mx-auto">
      <!-- Header -->
      <div class="text-center mb-12">
        <h1 class="text-4xl font-bold mb-4 bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent">
          Documentação
        </h1>
        <p class="text-xl text-gray-400 max-w-3xl mx-auto">
          Explore todos os métodos e funcionalidades disponíveis no Helper.DB
        </p>
      </div>

      <!-- Search Bar -->
      <div class="mb-12">
        <div class="max-w-2xl mx-auto relative">
          <input 
            type="text" 
            placeholder="Buscar métodos, categorias..."
            class="w-full bg-gray-800 border border-gray-700 rounded-lg px-4 py-3 pl-12 focus:outline-none focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
          >
          <div class="absolute left-4 top-1/2 transform -translate-y-1/2">
            <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
            </svg>
          </div>
        </div>
      </div>

      <!-- Categories Grid -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {docsStructure.map((category) => (
          <div class="bg-gray-800/80 backdrop-blur-sm rounded-xl border border-gray-700 overflow-hidden hover:border-gray-600 transition-colors">
            <div class="p-6 border-b border-gray-700">
              <div class="flex items-center mb-3">
                <span class="text-2xl mr-3">{category.icon}</span>
                <h2 class="text-xl font-semibold text-white">{category.title}</h2>
              </div>
              <p class="text-gray-400">{category.description}</p>
            </div>
            <div class="p-6">
              <div class="grid grid-cols-1 gap-2">
                {category.items.map((item) => (
                  <a 
                    href={`/docs/${category.slug}/${item.slug}`}
                    class="flex items-center justify-between p-3 rounded-lg hover:bg-gray-700/50 transition-colors group"
                  >
                    <span class="text-gray-300 group-hover:text-white transition-colors">
                      {item.title}
                    </span>
                    <svg class="w-4 h-4 text-gray-500 group-hover:text-blue-400 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                  </a>
                ))}
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </main>
</Layout>
